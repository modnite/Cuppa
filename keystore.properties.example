# Copy this file to keystore.properties (same directory) and fill in your real values.
# keystore.properties is gitignored — never commit it or the .jks file it points to.
#
# In CI (GitHub Actions), these same four values are supplied via secrets as environment
# variables instead of this file — see .github/workflows/release.yml.

storeFile=C:/Users/c/Desktop/Cuppa_Keystore/cuppa.jks
storePassword=
keyAlias=cuppa
keyPassword=
